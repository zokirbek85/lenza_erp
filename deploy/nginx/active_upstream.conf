# ========================================
# Active Upstream Configuration
# This file defines which stack (blue or green) is currently active
# It is updated automatically by the deployment scripts
# ========================================

# Default: BLUE stack
upstream active_backend {
    server lenza_backend_blue:8000;
}

upstream active_frontend {
    server lenza_frontend_blue:80;
}
